<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="32dp">

    <!-- Gambar folder sebagai latar -->
    <ImageView
        android:id="@+id/folderImage"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:contentDescription="Gambar folder perkuliahan"
        android:src="@drawable/fragmenperkuliahan"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintDimensionRatio="1150:700" />

    <!-- Tombol di tengah folderImage -->
    <ImageButton
        android:id="@+id/openFileButton"
        android:layout_width="500dp"
        android:layout_height="300dp"
        android:layout_marginStart="100dp"
        android:layout_marginTop="170dp"
        android:layout_marginEnd="100dp"
        android:layout_marginBottom="120dp"
        android:background="@null"
        android:contentDescription="Open File"
        android:scaleType="fitXY"
        android:src="@drawable/buttonperkuliahan"
        app:layout_constraintBottom_toBottomOf="@id/folderImage"
        app:layout_constraintEnd_toEndOf="@id/folderImage"
        app:layout_constraintHorizontal_bias="0.555"
        app:layout_constraintStart_toStartOf="@id/folderImage"
        app:layout_constraintTop_toTopOf="@id/folderImage"
        app:layout_constraintVertical_bias="0.0" />
</androidx.constraintlayout.widget.ConstraintLayout>
